# Server-side default LLM configuration
# When set, users can leave the corresponding fields blank in the UI
# and the server will fall back to these values.
# Copy this file to .env and fill in your values.

DEFAULT_API_URL=https://api.openai.com
DEFAULT_API_KEY=sk-your-key-here
DEFAULT_MODEL=gpt-4o
